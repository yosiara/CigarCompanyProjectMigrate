<odoo>
  <data>
    <!-- explicit list view definition -->

    <!-- <record model="ir.ui.view" id="smoke.list">
      <field name="name">fuma list</field>
      <field name="model">smoke</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="smoke.list2">
      <field name="name">fuma list</field>
      <field name="model">smoke</field>
      <field name="arch" type="xml">
        <form>
          <field name="name"/>
        </form>
      </field>
    </record>
z
    <record model="ir.actions.act_window" id="smoke.action_window">
      <field name="name">Smoke</field>
      <field name="res_model">smoke</field>
      <field name="view_mode">tree,form</field>
    </record> -->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="fuma.action_server">
      <field name="name">fuma server</field>
      <field name="model_id" ref="model_fuma_fuma"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
    <!-- <menuitem name="Smoke" id="smoke.menu_root"/> -->
    <!-- <menuitem name="Smoke" id="smoke.menu_action_root" action="smoke.action_window"/> -->

    <!-- menu categories -->
    <!-- <menuitem name="Reports" id="smoke.menu_category" parent="smoke.menu_action_root"/> -->
    <menuitem name="Smoke Report" id="smoke.menu_action" parent="hr.hr_menu_hr_reports"
      action="report_act_window_wzd"/>

<!--  
    <menuitem name="Server to list" id="fuma" parent="fuma.menu_2"
              action="fuma.action_server"/>
-->
  </data>
</odoo>
