<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="prorogation_view_list" model="ir.ui.view">
        <field name="name">planning.turei.prorogation.list</field>
        <field name="model">planning_turei.prorogation</field>
        <field name="arch" type="xml">
            <list string="Prórrogas" editable="bottom">
                <field name="request_date"/>
                <field name="cause"/>
                <field name="proposed_date"/>
                <field name="is_approved" widget="boolean_toggle"/>
            </list>
        </field>
    </record>
</odoo>